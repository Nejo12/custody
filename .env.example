# Supabase Configuration
# Copy this file to .env.local and fill in your actual values
# .env.local is gitignored and used for local development

# Supabase Project URL (from Settings → API in your Supabase dashboard)
SUPABASE_URL=your-project-url-here

# Supabase Service Role Key (from Settings → API → service_role key)
# Use service_role key for server-side API routes (bypasses RLS)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Alternative: If you prefer using public environment variables
# NEXT_PUBLIC_SUPABASE_URL=your-project-url-here
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# OpenAI/AI API Configuration (if needed for other features)
# OPENAI_API_KEY=your-openai-api-key-here
# OPENAI_API_BASE=https://api.openai.com/v1
# OPENAI_MODEL=gpt-4o-mini
# OPENAI_TRANSCRIBE_MODEL=gpt-4o-mini-transcribe
